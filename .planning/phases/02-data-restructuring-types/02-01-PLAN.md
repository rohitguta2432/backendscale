---
phase: 02-data-restructuring-types
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/index.ts
  - src/types/blog.ts
  - src/types/project.ts
  - src/types/service.ts
  - src/types/github.ts
  - src/types/testimonial.ts
  - src/types/k6.ts
  - src/types/reliability.ts
  - package.json
autonomous: true
requirements: [QUAL-03]

must_haves:
  truths:
    - "All data structures have Zod schemas that validate shape at runtime"
    - "TypeScript types are inferred from Zod schemas (single source of truth)"
    - "Types directory exists with barrel export"
  artifacts:
    - path: "src/types/index.ts"
      provides: "Barrel re-export of all types and schemas"
    - path: "src/types/blog.ts"
      provides: "BlogPost Zod schema and inferred type"
      exports: ["blogPostSchema", "BlogPost", "blogSectionSchema"]
    - path: "src/types/project.ts"
      provides: "Project Zod schema and inferred type"
      exports: ["projectSchema", "Project"]
    - path: "src/types/service.ts"
      provides: "Service Zod schema and inferred type"
      exports: ["serviceSchema", "Service"]
    - path: "src/types/github.ts"
      provides: "Repository, Contribution, ProjectNote Zod schemas and inferred types"
      exports: ["repositorySchema", "Repository", "contributionSchema", "Contribution", "projectNoteSchema", "ProjectNote"]
    - path: "src/types/testimonial.ts"
      provides: "Testimonial Zod schema and inferred type"
      exports: ["testimonialSchema", "Testimonial"]
    - path: "src/types/k6.ts"
      provides: "TestResult Zod schema and inferred type"
      exports: ["testResultSchema", "TestResult"]
    - path: "src/types/reliability.ts"
      provides: "CardConfig Zod schema and inferred type"
      exports: ["cardConfigSchema", "CardConfig"]
  key_links:
    - from: "src/types/*.ts"
      to: "src/types/index.ts"
      via: "barrel re-export"
      pattern: "export.*from"
---

<objective>
Install Zod and create the centralized types directory with Zod schemas for all data structures in the project.

Purpose: Establish the type foundation that Plans 02 and 03 will import from. Zod schemas are the single source of truth -- TypeScript types are inferred via `z.infer<>`, ensuring runtime validation and static types never drift.
Output: `src/types/` directory with 7 schema files + barrel index, Zod installed in package.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-restructuring-types/02-RESEARCH.md

@src/data/blog-posts.ts (lines 1-12 for BlogPost interface)
@src/data/projects.ts (lines 1-20 for Project interface)
@src/data/services.ts (lines 1-16 for Service interface)
@src/data/github.ts (lines 1-40 for Repository, Contribution, ProjectNote interfaces)
@src/components/Testimonials.tsx (lines 4-23 for testimonial shape)
@src/components/K6Results.tsx (lines 5-14 for TestResult interface)
@src/components/ReliabilitySection.tsx (lines 11-43 for CardConfig interface)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Zod and create all type/schema files</name>
  <files>package.json, src/types/blog.ts, src/types/project.ts, src/types/service.ts, src/types/github.ts, src/types/testimonial.ts, src/types/k6.ts, src/types/reliability.ts, src/types/index.ts</files>
  <read_first>
    - src/data/blog-posts.ts (lines 1-12 -- existing BlogPost interface)
    - src/data/projects.ts (lines 1-20 -- existing Project interface)
    - src/data/services.ts (lines 1-16 -- existing Service interface)
    - src/data/github.ts (lines 1-40 -- existing Repository, Contribution, ProjectNote interfaces)
    - src/components/Testimonials.tsx (lines 4-23 -- testimonial data shape)
    - src/components/K6Results.tsx (lines 5-14 -- existing TestResult interface)
    - src/components/ReliabilitySection.tsx (lines 11-43 -- existing CardConfig interface)
    - package.json
  </read_first>
  <action>
1. Run `npm install zod` to add Zod as a dependency.

2. Create `src/types/blog.ts`:
```typescript
import { z } from 'zod';

const blogSectionSchema = z.object({
  heading: z.string().min(1),
  content: z.string().min(1),
  image: z.object({
    src: z.string(),
    alt: z.string(),
  }).optional(),
});

export const blogPostSchema = z.object({
  slug: z.string().min(1),
  title: z.string().min(1),
  date: z.string().regex(/^\d{4}-\d{2}-\d{2}$/),
  excerpt: z.string().min(1),
  readingTime: z.string(),
  keywords: z.array(z.string()),
  relatedProject: z.string().optional(),
  coverImage: z.object({
    src: z.string(),
    alt: z.string(),
  }).optional(),
  sections: z.array(blogSectionSchema).min(1),
  cta: z.object({
    text: z.string(),
    href: z.string(),
  }).optional(),
});

export type BlogPost = z.infer<typeof blogPostSchema>;
```

3. Create `src/types/project.ts`:
```typescript
import { z } from 'zod';

export const projectSchema = z.object({
  slug: z.string().min(1),
  name: z.string().min(1),
  problem: z.string().min(1),
  solves: z.string().min(1),
  techStack: z.array(z.string()),
  status: z.enum(['active', 'iterating', 'paused']),
  repoUrl: z.string().optional(),
  aiApproach: z.string().optional(),
  image: z.string().optional(),
  images: z.array(z.object({
    src: z.string(),
    caption: z.string(),
  })).optional(),
  details: z.object({
    businessImpact: z.string(),
    approach: z.array(z.string()),
    decisions: z.array(z.string()),
    currentStatus: z.string(),
    roadmap: z.array(z.string()),
    improvements: z.array(z.string()),
  }),
});

export type Project = z.infer<typeof projectSchema>;
```

4. Create `src/types/service.ts`:
```typescript
import { z } from 'zod';

export const serviceSchema = z.object({
  slug: z.string().min(1),
  title: z.string().min(1),
  metaTitle: z.string().min(1),
  metaDescription: z.string().min(1),
  headline: z.string().min(1),
  subheadline: z.string().min(1),
  problem: z.string().min(1),
  whatYouGet: z.array(z.string()),
  techStack: z.array(z.string()),
  timeline: z.string(),
  costRange: z.string(),
  portfolioSlugs: z.array(z.string()),
  faqs: z.array(z.object({
    question: z.string(),
    answer: z.string(),
  })),
  cta: z.string(),
});

export type Service = z.infer<typeof serviceSchema>;
```

5. Create `src/types/github.ts`:
```typescript
import { z } from 'zod';

export const repositorySchema = z.object({
  name: z.string().min(1),
  fullName: z.string().min(1),
  description: z.string().nullable(),
  language: z.string().nullable(),
  url: z.string(),
  createdAt: z.string(),
  updatedAt: z.string(),
  status: z.enum(['active', 'maintenance', 'archived']),
  isFork: z.boolean(),
  modules: z.array(z.string()).optional(),
  problemSolved: z.string().optional(),
  contributionScope: z.string().optional(),
});

export type Repository = z.infer<typeof repositorySchema>;

export const contributionSchema = z.object({
  title: z.string().min(1),
  type: z.enum(['open-source', 'experiment', 'ai-systems']),
  repo: z.string(),
  repoUrl: z.string(),
  summary: z.string(),
  impact: z.string().optional(),
  modules: z.array(z.string()).optional(),
  date: z.string(),
});

export type Contribution = z.infer<typeof contributionSchema>;

export const projectNoteSchema = z.object({
  slug: z.string().min(1),
  projectName: z.string().min(1),
  whyExists: z.string(),
  coreTechnicalChallenge: z.string(),
  architectureSnapshot: z.array(z.string()),
  tradeoffs: z.array(z.string()),
  currentState: z.string(),
  repoUrl: z.string(),
  category: z.enum(['ai-systems', 'backend-infrastructure', 'frontend', 'experiments']),
});

export type ProjectNote = z.infer<typeof projectNoteSchema>;
```

6. Create `src/types/testimonial.ts`:
```typescript
import { z } from 'zod';

export const testimonialSchema = z.object({
  name: z.string().min(1),
  role: z.string().min(1),
  text: z.string().min(1),
  project: z.string().min(1),
});

export type Testimonial = z.infer<typeof testimonialSchema>;
```

7. Create `src/types/k6.ts`:
```typescript
import { z } from 'zod';

export const testResultSchema = z.object({
  scenario: z.string().min(1),
  vus: z.number().int().positive(),
  duration: z.string(),
  requests: z.number().int().nonnegative(),
  rps: z.number().nonnegative(),
  p95: z.number().nonnegative(),
  p99: z.number().nonnegative(),
  errorRate: z.number().nonnegative(),
});

export type TestResult = z.infer<typeof testResultSchema>;
```

8. Create `src/types/reliability.ts`:
```typescript
import { z } from 'zod';

export const cardConfigSchema = z.object({
  key: z.enum(['observability', 'loadTesting', 'apiTesting', 'kafkaTesting']),
  icon: z.string().min(1),
  route: z.string().min(1),
  accentColor: z.string().regex(/^#[0-9a-fA-F]{6}$/),
});

export type CardConfig = z.infer<typeof cardConfigSchema>;
```

9. Create `src/types/index.ts` barrel:
```typescript
// Blog
export { blogPostSchema } from './blog';
export type { BlogPost } from './blog';

// Project
export { projectSchema } from './project';
export type { Project } from './project';

// Service
export { serviceSchema } from './service';
export type { Service } from './service';

// GitHub
export { repositorySchema, contributionSchema, projectNoteSchema } from './github';
export type { Repository, Contribution, ProjectNote } from './github';

// Testimonial
export { testimonialSchema } from './testimonial';
export type { Testimonial } from './testimonial';

// K6 Results
export { testResultSchema } from './k6';
export type { TestResult } from './k6';

// Reliability
export { cardConfigSchema } from './reliability';
export type { CardConfig } from './reliability';
```
  </action>
  <verify>
    <automated>cd /home/t0266li/Documents/nexusai && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "zod" in dependencies
    - src/types/index.ts exists and contains `export { blogPostSchema }` and `export { projectSchema }` and `export { serviceSchema }` and `export { repositorySchema }` and `export { testimonialSchema }` and `export { testResultSchema }` and `export { cardConfigSchema }`
    - src/types/blog.ts contains `export const blogPostSchema = z.object(`
    - src/types/blog.ts contains `export type BlogPost = z.infer<typeof blogPostSchema>`
    - src/types/project.ts contains `status: z.enum(['active', 'iterating', 'paused'])`
    - src/types/service.ts contains `export const serviceSchema = z.object(`
    - src/types/github.ts contains `export const repositorySchema` and `export const contributionSchema` and `export const projectNoteSchema`
    - src/types/testimonial.ts contains `export const testimonialSchema = z.object(`
    - src/types/k6.ts contains `export const testResultSchema = z.object(`
    - src/types/reliability.ts contains `key: z.enum(['observability', 'loadTesting', 'apiTesting', 'kafkaTesting'])`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All 7 type/schema files + barrel index created in src/types/. Zod installed. Build passes. Types inferred from Zod schemas (single source of truth pattern).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Data files to rendering | Static data files are developer-authored, not user-supplied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | src/types/*.ts schemas | accept | Data files are developer-authored and version-controlled. Zod validates shape at module load time, catching accidental malformation. No user-supplied data enters this path. |
| T-02-02 | Information Disclosure | Zod error messages | accept | Validation errors log to server console only (module-load time). No validation errors are exposed to end users. |
</threat_model>

<verification>
- `npm run build` succeeds (TypeScript compilation validates all type files)
- `npm run lint` passes
- All 8 files exist in src/types/
</verification>

<success_criteria>
- src/types/ directory exists with blog.ts, project.ts, service.ts, github.ts, testimonial.ts, k6.ts, reliability.ts, index.ts
- Every schema file uses `z.infer<typeof schema>` pattern for type export
- Zod is in package.json dependencies
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-restructuring-types/02-01-SUMMARY.md`
</output>
