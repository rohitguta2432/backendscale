---
phase: 01-dark-mode-credential-security
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
autonomous: true
gap_closure: true
requirements:
  - VISL-01

must_haves:
  truths:
    - "--card-bg CSS variable is defined in both light and dark :root blocks"
    - ".btn-primary and .skip-link text is readable in dark mode (not white-on-near-white)"
  artifacts:
    - path: "src/app/globals.css"
      provides: "--card-bg variable and dark mode button/skip-link contrast fix"
      contains: "--card-bg"
  key_links:
    - from: "src/app/globals.css"
      to: "components using var(--card-bg)"
      via: "CSS custom property inheritance"
      pattern: "--card-bg"
---

<objective>
Fix two verification gaps from Phase 1:
1. Add missing --card-bg CSS variable to both light and dark :root blocks
2. Fix .btn-primary and .skip-link contrast in dark mode (white text on near-white background)

These are targeted fixes to globals.css — no other files need changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-dark-mode-credential-security/01-VERIFICATION.md
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --card-bg variable and fix button/skip-link dark mode contrast</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (current :root blocks, .btn-primary, .skip-link selectors)
  </read_first>
  <action>
Make three changes to src/app/globals.css:

1. **Add --card-bg to the light-mode :root block** (around line 3-20):
   Add `--card-bg: #ffffff;` after the existing `--surface-hover` variable.

2. **Add --card-bg to the dark-mode :root block** (inside @media (prefers-color-scheme: dark)):
   Add `--card-bg: #1a1a1a;` after the existing `--surface-hover` variable.

3. **Fix .btn-primary dark mode contrast** (around line 195-203):
   The `.btn-primary` selector currently uses `color: white`. Add a dark mode override block after the existing `.btn-primary` rule:

   ```css
   @media (prefers-color-scheme: dark) {
     .btn-primary {
       color: var(--bg);
     }
     .skip-link:focus {
       color: var(--bg);
     }
   }
   ```

   This changes the text color from hardcoded white to var(--bg) which is #0a0a0a in dark mode, providing strong contrast against the #f5f5f5 background.

Do NOT modify any existing selectors or values — only add the new variable declarations and the new dark mode override block.
  </action>
  <verify>
    <automated>grep -c "\-\-card-bg" src/app/globals.css | grep -q "[2-9]" && grep "btn-primary" -A 3 src/app/globals.css | grep -q "var(--bg)" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\-\-card-bg" src/app/globals.css` returns at least 2 (light + dark :root)
    - Light :root contains `--card-bg: #ffffff`
    - Dark :root contains `--card-bg: #1a1a1a`
    - A dark mode override for .btn-primary exists with `color: var(--bg)`
    - A dark mode override for .skip-link:focus exists with `color: var(--bg)`
    - `npm run build` passes without errors
  </acceptance_criteria>
  <done>--card-bg defined in both :root blocks, .btn-primary and .skip-link have readable text in dark mode</done>
</task>

</tasks>

<verification>
1. `grep "\-\-card-bg: #ffffff" src/app/globals.css` matches in light :root
2. `grep "\-\-card-bg: #1a1a1a" src/app/globals.css` matches in dark :root
3. Dark mode .btn-primary uses `color: var(--bg)` not `color: white`
4. `npm run build` passes
</verification>

<success_criteria>
- --card-bg exists in both light and dark :root blocks
- .btn-primary and .skip-link text readable in dark mode
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-dark-mode-credential-security/01-03-SUMMARY.md`
</output>
